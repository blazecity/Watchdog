<UserControl x:Class="Watchdog.Forms.Settings.UserControlAssetClasses"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:self="clr-namespace:Watchdog.Entities"
             xmlns:local="clr-namespace:Watchdog.Forms.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Margin="0 0 30 0" Content="Name der Assetklasse"></Label>
            <TextBox Margin="0 0 30 0" HorizontalAlignment="Left" MinWidth="200" ></TextBox>
            <Button Margin="0 0 30 0" Content="Assetklasse hinzufügen" Width="200" HorizontalAlignment="Left"></Button>
        </StackPanel>
        <DataGrid >
            <DataGrid.Resources>
                <DataTemplate DataType=""></DataTemplate>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Assetklasse" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>            
    </StackPanel>
</UserControl>
